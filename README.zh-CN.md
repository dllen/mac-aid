# Mac Aid：Homebrew 命令助手

一个基于 Rust 的 TUI 应用，帮助你发现并理解通过 Homebrew 安装的命令行工具。程序会将本地的 man/help 文档索引到向量库，并使用本地 Ollama 模型根据你的需求推荐工具和示例用法。

## 功能

- 包发现：自动枚举已安装的 Homebrew formula
- AI 推荐：结合你的需求给出最合适的工具与用法示例
- 文档检索：利用向量库检索相关 man/help 内容（RAG）
- 交互界面：基于 Ratatui 的简洁终端界面
- 命令行直出：支持在命令行直接发起一次查询并输出结果

## 先决条件

1. Rust（1.70+）
2. Homebrew
3. Ollama（本地 LLM 运行时）
4. 模型：生成模型与嵌入模型（默认：`qwen3-coder:480b-cloud` + `all-minilm`）

示例：
```bash
brew install ollama
ollama pull all-minilm
ollama pull qwen3-coder:480b-cloud
```

## 安装与运行

```bash
git clone <repo_url>
cd mac-aid
cargo build --release
cargo run --release
```

## 使用

### 交互模式快捷键

- Enter：提交查询
- Esc：清空输入
- q：退出
- Ctrl + r：后台重建知识库
- Shift + R：后台重载索引数据
- ↑/↓：滚动查看结果

### 命令行一次性查询

无需进入 TUI，直接在命令行请求并输出结果（不使用 RAG 检索）：

```bash
mac-aid how to compress file
mac-aid "convert image format"
```

### 界面布局

```
┌──────────────────────────────────────────────┐
│ 🔍 Enter your need                           │
│ [type your query here]                       │
└──────────────────────────────────────────────┘
┌──────────────────────────────────────────────┐
│ Status: Ready / Indexing progress messages   │
└──────────────────────────────────────────────┘
┌──────────────────────────────────────────────┐
│ 💡 Recommendation                            │
│ AI suggestions and usage examples appear here│
└──────────────────────────────────────────────┘
```

## 配置

### 模型设置

交互生成模型在 `src/main.rs` 中初始化；同时模型也写入到配置文件：

配置文件路径：`~/.mac-aid/config.json`（首次运行自动创建）

示例内容：
```json
{
  "ollama_model": "qwen3-coder:480b-cloud",
  "embedding_model": "all-minilm"
}
```

### 自定义 Ollama 地址

如需修改主机或端口，可编辑 `src/ollama.rs` 中 `base_url` 字段。

## 非阻塞重建/重载

- 重建和重载在后台执行，不会阻塞主界面。
- 在后台执行期间发起的查询会跳过向量检索（RAG），直接使用本地 Ollama 给出结果。

## 数据位置

- 数据库：`~/.mac-aid/commands.db`
- 日志：`~/.mac-aid/error.log`、`~/.mac-aid/info.log`

## 项目结构

```
src/
├── main.rs
├── app.rs
├── ui.rs
├── brew.rs
├── indexer.rs
├── vector_store.rs
├── rag.rs
├── langchain_integration.rs
├── kb_builder.rs
├── config.rs
└── log.rs
```

## 开发

```bash
cargo build
cargo run
cargo test
```

## 许可

MIT

